{
  "build": {
    "dockerfile": "../docker/cursor/Dockerfile",
    "context": ".."
  },
  "start": "sudo service docker start",
  "install": "sudo service docker start && sleep 3 && docker compose up -d --wait"
}
